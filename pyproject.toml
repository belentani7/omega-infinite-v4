[project]
name = "omega-infinite-v4"
version = "1.0.0"
description = "Omega Infinite OS Core API"
requires-python = ">=3.9"
dependencies = [
  "fastapi",
  "uvicorn",
  "psutil",
]

[tool.vercel]
entrypoint = "backend:app"